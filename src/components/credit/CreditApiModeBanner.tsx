'use client'

/** Bannière mode crédit désactivée — pas d’indication mock/API dans l’interface. */
export function CreditApiModeBanner(_props: {
  source?: 'mock' | 'api'
  compact?: boolean
}) {
  return null
}

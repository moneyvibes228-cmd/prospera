'use client'

/** Bannière source données désactivée — pas d’indication mock/API dans l’interface. */
export function PhasesAdDataBanner(_props: {
  source?: 'mock' | 'api'
  endpoint?: string
  compact?: boolean
  extra?: string
}) {
  return null
}
